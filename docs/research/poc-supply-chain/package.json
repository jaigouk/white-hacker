{
  "name": "poc-supply-chain-demo",
  "version": "0.0.0",
  "private": true,
  "description": "NEUTRALIZED demo project for the deps-scan supply-chain-malware floor (wh-07w). Contains ONE bad package (a typosquat of 'express') plus an INERT trigger postinstall script and a benign pinned control dep. All detection-trigger strings are clearly inert/commented — this is detection test data, NEVER functional malware.",
  "dependencies": {
    "expresss": "1.0.0",
    "react": "18.2.0"
  },
  "scripts": {
    "postinstall": "node scripts/postinstall.js"
  }
}
