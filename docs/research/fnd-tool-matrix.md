# Security Tool Matrix (2026)

> Source: wycjclbk6 agent `synth:tool-matrix`. 23 tools.

| Tier | Name | Category | Languages | Cost | MCP | Install | Run | When |
|---|---|---|---|---|---|---|---|---|
| core-default | Opengrep | SAST | TS/JS, Go, Python, Java (12 langs total) | OSS | no (run via CLI/Bash) | `brew install opengrep/tap/opengrep  (or curl -sSfL https://raw.githubusercontent.com/opengrep/opengrep/main/install.sh | bash)` | `opengrep scan --config auto --sarif-output results.sarif .` | Default first-pass SAST for any polyglot repo. One binary, Semgrep-compatible rules, free taint/interprocedural/fingerprinting (the features Semgrep moved to paid). LGPL-2.1, 10+ vendor consortium; v1.16.5 Mar 2026. Use when you want zero-login OSS pattern+taint coverage across all four languages. |
| core-default | Trivy | SCA/deps | language-agnostic (npm/pip/Go/Maven + OS pkgs, IaC, secrets, SBOM) | OSS | yes — trivy plugin install mcp; then `trivy mcp`; claude mcp add trivy -- trivy mcp. v0.0.20 (Dec 2025). Exposes 6 tools: scan_filesystem, scan_image, scan_repository, trivy_version, findings_list, findings_get (mcp__trivy__*) | `brew install trivy   (PIN to a clean version: v0.71.0 / v0.70.0; AVOID binary v0.69.4 and Docker 0.69.5/0.69.6 — malicious. Do NOT auto-install from unpinned sources; verify GPG/digest)` | `trivy fs --scanners vuln,misconfig,secret,license --severity HIGH,CRITICAL --ignore-unfixed --format json .   (also trivy image / trivy config / trivy repo)` | Best single-binary all-rounder: SCA + IaC/misconfig + secrets + SBOM + license across containers/IaC/all four languages. Use as SCA/IaC core when containers or Terraform/K8s matter. Has NO SAST — pair with Opengrep/Semgrep. 2026 supply-chain history: pin version, pin actions to commit SHA, pin images to digest. |
| core-default | OSV-Scanner | SCA/deps | language-agnostic (11+ langs / 19+ lockfiles) | OSS | no (run via CLI/Bash) | `go install github.com/google/osv-scanner/v2/cmd/osv-scanner@latest  (or brew install osv-scanner)` | `osv-scanner scan --format sarif --output results.sarif .   (osv-scanner fix for guided remediation)` | Best vendor-neutral lockfile/manifest SCA. v2.3.5 (Mar 2026), Apache-2.0, OSV.dev data, SARIF, built-in `fix`; v2 added transitive Python requirements.txt scanning. Use this OR Trivy as the SCA core — Trivy when containers/IaC matter, OSV-Scanner for a pure lockfile-first language SCA with cleaner remediation. |
| core-default | Gitleaks | secrets | language-agnostic | OSS | no (run via CLI/Bash) | `brew install gitleaks` | `gitleaks detect --source . --report-format sarif --report-path gitleaks.sarif   (or `gitleaks dir .`)` | Fast pattern-based secret pass; ideal for pre-commit and a cheap first sweep. MIT, latest v8.30.1, but FEATURE-COMPLETE as of 2026 (security patches only) and has NO live verification. Use for speed; pair with TruffleHog in CI for confirmation. |
| core-default | TruffleHog | secrets | language-agnostic (800+ detectors, non-git sources) | OSS | no (run via CLI/Bash) | `brew install trufflehog  (or curl -sSfL https://raw.githubusercontent.com/trufflesecurity/trufflehog/main/scripts/install.sh | sh -s -- -b /usr/local/bin)` | `trufflehog filesystem . --results=verified --json   (also git/s3/docker sources)` | Differentiator is LIVE credential verification across 800+ detectors and non-git sources (S3, Docker, Slack). Run in CI with --results=verified to collapse noise to confirmed-active creds. Use as the verification layer behind Gitleaks; especially relevant for AI repos with model/cloud credentials. |
| core-default | govulncheck | language-native | Go | OSS | no (run via CLI/Bash) | `go install golang.org/x/vuln/cmd/govulncheck@latest` | `govulncheck ./...` | Near-zero-cost, reachability-aware Go gate: reports only vulnerable symbols actually reachable in the call graph, so very low false-positive. Run weekly/in CI for any Go project. Reachability beats presence — prefer this over generic SCA for Go dep gating. |
| core-default | ruff (S rules) | language-native | Python | OSS | no (run via CLI/Bash) | `uv tool install ruff  (or pip install ruff)` | `ruff check --select S .` | Fast Python security gate via flake8-bandit 'S' rules — 10-100x faster than bandit, shares the AST. Default Python security linter for speed. Keep bandit too for framework/AI code (see bandit row). |
| core-default | govulncheck/pip-audit/npm audit/cargo-audit | language-native | Go / Python / JS / Rust | OSS | no (run via CLI/Bash) | `go install golang.org/x/vuln/cmd/govulncheck@latest ; pip install pip-audit ; (npm/pnpm audit bundled) ; cargo install cargo-audit` | `pip-audit ; npm audit --omit=dev ; cargo audit ; govulncheck ./...` | Native, in-toolchain dependency gates that cost almost nothing and run identically local/CI. Use these as the first SCA gate per language before reaching for Trivy/OSV-Scanner; govulncheck and cargo-audit add reachability for low noise. |
| core-default | OWASP Top 10 (2025) + LLM/MCP/Agentic Top 10 (reference) | LLM/AI | language-agnostic (mapping standard, not a binary) | OSS | n/a | `n/a (knowledge reference — Read/Grep against source)` | `map findings to: OWASP Top 10:2025 (web), API Security Top 10 2023 (APIs, NO 2025/26 edition), LLM Apps Top 10 2025, MCP Top 10 (beta), Agentic Apps Top 10 (Dec 2025)` | Use as the severity/category mapping backbone. Built-in Read/Grep/Glob over the target dir is a sufficient read-only scaffold for AI/LLM checks: improper output handling (LLM05 — model/tool output into eval/exec/SQL/HTML/path/URL/template/deserializer), lethal trifecta / Agents Rule of Two, prompt-injection (no reliable fix — architectural defenses only), MCP token-passthrough/tool-poisoning, RAG poisoning, unbounded consumption. No external tool required. |
| optional | detect-secrets | secrets | language-agnostic | OSS | no (run via CLI/Bash) | `pip install detect-secrets  (uv run: uv tool install detect-secrets)` | `detect-secrets scan > .secrets.baseline   (then detect-secrets audit .secrets.baseline)` | Pick when onboarding a brownfield/legacy repo with existing secrets in history — its .secrets.baseline model suppresses alert floods so you can ratchet. Yelp, Apache-2.0. Optional unless legacy secrets exist. |
| optional | eslint-plugin-security | language-native | JS/TS | OSS | no (run via CLI/Bash) | `npm i -D eslint eslint-plugin-security` | `npx eslint . --plugin security` | In-editor JS/TS hygiene pass (flags eval/new Function, child_process, unsafe regex). Pattern/regex-level, NOT dataflow-aware — the 2026 consensus is ESLint in-editor + Opengrep/Semgrep in CI for real JS/TS security depth. Don't rely on it alone. |
| optional | Snyk | SCA/deps | TS/JS, Go, Python, Java (SCA + SAST + IaC + containers) | freemium | yes — official Claude Code MCP (npx snyk mcp configure --tool=claude-cli), includes an autofix loop | `npm i -g snyk  (then: npx snyk mcp configure --tool=claude-cli  /  npx -y snyk@latest mcp configure --tool=claude-cli)` | `snyk test ; snyk code test ; snyk iac test  (autofix loop via MCP)` | Optional escalation when the user ALREADY pays for Snyk. Proprietary with a tight free tier (200 OSS tests/mo). Strength = reachability for Java/JS + autofix MCP. Not a vendor-neutral default for a portable agent — opt in only when licensed. |
| optional | SonarQube (MCP) | SAST | TS/JS, Go, Python, Java (many langs) | freemium | yes — official SonarSource MCP (Docker image mcp/sonarqube; SonarQube Cloud also ships an embedded native MCP in 2026) | `claude mcp add sonarqube --env SONARQUBE_TOKEN=$SONAR_TOKEN -- docker run -i --rm --init --pull=always -e SONARQUBE_TOKEN mcp/sonarqube` | `via MCP (~25 tools) against a SonarQube/SonarCloud instance` | Optional escalation for teams already on SonarQube. NOTE: Community Build (LGPL-3.0) LACKS taint/injection SAST and branch analysis — real security SAST starts at paid Developer Edition / SonarQube Cloud. Don't treat the free self-hosted build as security-grade. |
| optional | CodeQL | SAST | TS/JS, Go, Python, Java | freemium | no (run via CLI/Bash) | `download CodeQL CLI bundle from github.com/github/codeql-action releases (or gh extension)` | `codeql database create db --language=java && codeql database analyze db --format=sarif-latest -o results.sarif` | Deepest dataflow/taint analysis. LICENSE TRAP: CLI is free only for public/OSS/academic code — private-repo local scanning needs paid GitHub Advanced Security. Treat as a public-repo-only deep-dive or a GHAS-customer option; not a default for private polyglot repos. |
| optional | PromptFoo / garak (LLM red-team) | LLM/AI | language-agnostic (LLM apps) | OSS | no (run via CLI/Bash) | `npm i -g promptfoo   (garak: pip install garak)` | `promptfoo redteam run   (garak --model_type huggingface --model_name <m>)` | Optional dynamic LLM/AI red-teaming: prompt-injection, jailbreak, data-leak, tool-misuse probes against a running model/agent. Use for AI projects when you can exercise the model behaviorally (the behavioral oracle for prompt-injection findings); pairs with the static LLM05 grep pass. |
| recommended | Semgrep CE | SAST | TS/JS, Go, Python, Java (30+ langs) | freemium | yes (uvx semgrep-mcp; claude mcp add semgrep -- uvx semgrep-mcp) — first-party plugin bundles MCP+hooks+skills | `brew install semgrep  (or pipx install semgrep / uv tool install semgrep)` | `semgrep scan --config auto --sarif -o results.sarif .` | Alternative/companion to Opengrep when you want the upstream registry (~3,000 rules) or the only first-party Claude Code MCP plugin. Note: cross-file dataflow and AI business-logic (IDOR/authz) detection are PAID (Team $35/contributor/mo). Pick Opengrep if you need free interprocedural taint; pick Semgrep CE if you specifically want its MCP plugin. |
| recommended | Checkov | IaC/container | Terraform, CloudFormation, K8s, Helm, ARM, Bicep, Ansible, Dockerfile | OSS | no (run via CLI/Bash) | `pip install checkov  (or docker run --rm -v "$(pwd):/tf" bridgecrew/checkov -d /tf)` | `checkov -d . --compact --output sarif --output-file-path .` | Run in PARALLEL with Trivy config for IaC: Checkov adds graph/cross-resource and compliance checks (1,000+ policies, v3.2.526 Apr 2026, Apache-2.0). Especially valuable now that tfsec is deprecated (merged into Trivy) and Terrascan was archived (Nov 2025). |
| recommended | hadolint | IaC/container | Dockerfile | OSS | no (run via CLI/Bash) | `brew install hadolint` | `hadolint Dockerfile` | Dockerfile lint pass: flags non-root USER, secret-in-layer, missing pinning, unsafe RUN bash. Cheap and language-agnostic. Use whenever a repo ships a Dockerfile. |
| recommended | zizmor | IaC/container | GitHub Actions workflows | OSS | no (run via CLI/Bash) | `brew install zizmor  (or uv tool install zizmor / cargo install zizmor)` | `zizmor .github/workflows/` | Static analysis for GitHub Actions: catches pwn-requests (pull_request_target/workflow_run checking out untrusted PR code), template/script injection via ${{ github.event.* }}, unpinned actions, over-broad permissions. Pair with actionlint. High-yield given 2026 Actions supply-chain attacks (tj-actions, Trivy action). |
| recommended | Grype + Syft | SCA/deps | language-agnostic (SBOM + vuln) | OSS | no (run via CLI/Bash) | `brew install syft grype` | `syft dir:. -o cyclonedx-json > sbom.json && grype sbom:sbom.json -o sarif` | Cleaner SBOM pipeline (syft -> CycloneDX/SPDX, grype scans it) and quieter than Trivy on RHEL/Ubuntu backport-patched packages. Use when you need a first-class SBOM artifact for provenance/attestation, or to cross-check noisy Trivy distro findings. |
| recommended | gosec | language-native | Go | OSS | no (run via CLI/Bash) | `go install github.com/securego/gosec/v2/cmd/gosec@latest  (bundled in golangci-lint v2)` | `gosec -fmt sarif -out gosec.sarif ./...` | CWE-mapped Go security linter; v2.26.1 with a taint engine (v2.23.0+) covering SQLi/cmd-injection/path-traversal/SSRF/SSTI/deserialization. Run alongside govulncheck. Treat G115 integer-overflow findings as advisory/needs-review (known FPs). |
| recommended | bandit | language-native | Python | OSS | no (run via CLI/Bash) | `pip install bandit  (uv tool install bandit)` | `bandit -r . -f sarif -o bandit.sarif` | Keep for framework/AI Python code (Django, raw SQLAlchemy, PyTorch, Hugging Face): ~6 bandit detectors have no ruff equivalent. Especially relevant for AI projects (pickle/torch.load/yaml.load, trust_remote_code). Use as a complement to ruff S-rules, not a replacement. |
| recommended | SpotBugs + find-sec-bugs | language-native | Java | OSS | no (run via CLI/Bash) | `brew install spotbugs  (add find-sec-bugs plugin jar)` | `spotbugs -pluginList findsecbugs-plugin.jar -sarif -output fsb.sarif target/classes` | Deepest free Java taint/security analysis (deserialization, XXE, SpEL, SQLi). OPERATIONAL GOTCHA: requires COMPILED bytecode — the agent must build the project first. Use PMD on source if only source is available. Reach for it on any Maven/Gradle Java service. |

