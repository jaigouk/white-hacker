# Rejected self-write proposals (pre-commit safety log)

Append-only. `precommit_safety.py` (T-9.4) records every blocked outer-loop proposal here with the
gate(s) it failed, so `sec-learn`/`sec-kb-refresh` never re-propose a known loser. Seeded empty.
