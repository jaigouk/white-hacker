{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://white-hacker/skills/sec-init/project-profile.schema.json",
  "title": "white-hacker project-scope companion profile",
  "description": "A GATED, project-scope companion the generic white-hacker agent consumes. It specializes detection-derived facts ONLY. additionalProperties:false at the top level GUARANTEES no shipped-identity key (posture, tools, tool-scope, output-contract, review-stages) can ever appear — init never rewrites the shipped identity (ADR-004, ADR-017).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "generated_note",
    "detected_langs",
    "frameworks",
    "present_capabilities",
    "tools_unavailable",
    "load_appendices",
    "ai_pass",
    "scoring_standard",
    "threat_model_seed",
    "security_policy"
  ],
  "properties": {
    "schema_version": {
      "type": "string"
    },
    "generated_note": {
      "type": "string",
      "description": "A factual one-line provenance statement (never an imperative instruction)."
    },
    "detected_langs": {
      "type": "array",
      "items": { "type": "string" }
    },
    "frameworks": {
      "type": "array",
      "items": { "type": "string" }
    },
    "present_capabilities": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Security capabilities backed by an installed tool (sast/sca/secrets/iac/ai-redteam)."
    },
    "tools_unavailable": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Capabilities with no installed tool; the agent degrades these to the Read/Grep/Glob floor."
    },
    "load_appendices": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Per-language reference appendices to load (lang-go/lang-python/lang-typescript/lang-java)."
    },
    "ai_pass": {
      "type": "boolean",
      "description": "True when LLM/AI deps are present; the AI/LLM review pass applies."
    },
    "scoring_standard": {
      "type": ["string", "null"],
      "description": "Severity scoring standard (e.g. CVSS or an org bug-bar). Defaults to null: must be human-confirmed, never hardcoded."
    },
    "threat_model_seed": {
      "type": "object",
      "additionalProperties": false,
      "required": ["assets", "entry_points", "trust_boundaries"],
      "properties": {
        "assets": {
          "type": "array",
          "items": { "type": "string" }
        },
        "entry_points": {
          "type": "array",
          "items": { "type": "string" }
        },
        "trust_boundaries": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },
    "security_policy": {
      "type": "object",
      "additionalProperties": false,
      "description": "Coordinated-disclosure policy facts detected by sec-init (SECURITY.md and/or an RFC 9116 security.txt). Structural facts only — booleans, a closed reporting-channel enum, and the repo-relative path; never any file-body span (the file is untrusted data).",
      "required": [
        "present",
        "path",
        "reporting_channel",
        "supported_versions_present",
        "disclosure_timeline_present",
        "security_txt_present",
        "security_txt_expired"
      ],
      "properties": {
        "present": {
          "type": "boolean",
          "description": "True when a SECURITY.md exists at one of the known locations."
        },
        "path": {
          "type": ["string", "null"],
          "description": "Repo-relative path of the detected SECURITY.md, or null when absent."
        },
        "reporting_channel": {
          "enum": ["github-pvr", "email", "url", "none", "unknown"],
          "description": "How vulnerabilities are reported: GitHub private vulnerability reporting, an email, a generic URL, none, or unknown."
        },
        "supported_versions_present": {
          "type": "boolean",
          "description": "True when the policy has a 'Supported Versions' section."
        },
        "disclosure_timeline_present": {
          "type": "boolean",
          "description": "True when the policy states a numeric coordinated-disclosure window."
        },
        "security_txt_present": {
          "type": "boolean",
          "description": "True when an RFC 9116 security.txt exists."
        },
        "security_txt_expired": {
          "type": ["boolean", "null"],
          "description": "True if the security.txt Expires field is in the past, False if valid, null when absent/unparseable."
        }
      }
    }
  }
}
