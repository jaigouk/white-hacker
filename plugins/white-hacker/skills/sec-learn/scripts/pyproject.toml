[project]
name = "sec-learn-scripts"
version = "0.1.0"
description = "Collate capture-hook traces into the sec-learn reflection input."
requires-python = ">=3.10"

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
